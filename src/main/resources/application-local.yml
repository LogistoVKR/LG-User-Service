spring:
  datasource:
    url: jdbc:postgresql://localhost:2345/mercurio
    username: mercurio
    password: mercurio
    hikari:
      minimum-idle: 1
      maximum-pool-size: 2
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/mercurio
